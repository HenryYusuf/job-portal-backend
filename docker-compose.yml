services:
  app:
    image: oven/bun:1.3.10
    container_name: job-portal-backend-dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: >
      sh -c "bun install && bun run dev"
